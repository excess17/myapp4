code: myapp4-bundle
description: This is the myapp4 bundle

components:
  plugins:
    - plugins/myapp4-plugin.yaml

  widgets:
    - ui/widgets/city/tableWidget/city-table-widget-descriptor.yaml
    - ui/widgets/city/detailsWidget/city-details-widget-descriptor.yaml
    - ui/widgets/city/formWidget/city-form-widget-descriptor.yaml
# entando-needle-descriptor-add-widgets - Entando blueprint will add widget bundle files here

